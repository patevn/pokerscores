// import * as types from '../actions/actionTypes';

// export default function totalsReducer(state = null, action) {
//   switch (action.type) {
//       case types.TOTAL:
//     let totalz = calculator(action.aPileOfState.all)
//      return Object.assign({}, state, { totals: totalz })
//     default:
//       return state;
//   }
// }
// //need to work out how to share state between reducers

// // function calculator(fwd) {
// //   fwd.kassie.currentData.forEach(element => {
// //     if (element.who == "Matty") {
// //       fwd.kassie.totals.Matty.cashWon = fwd.kassie.totals.Matty.cashWon + element.cashWon;
// //     }
// //   });
// // }