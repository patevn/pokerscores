import React from 'react';
import { Link } from 'react-router'

let Nav = ({ }) => (
    <div>
        <Link className="btn btn-success" to='/'   >Main Menu</Link>&nbsp;
     </div>
)

export default Nav